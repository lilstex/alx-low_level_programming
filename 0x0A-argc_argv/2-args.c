#include "holberton.h"

/**
* main - prints all arguments passed it receives
* @argc: argument count
* @argv: argument vector
* Return: 0
*/
int i;

	for (i = 0; i < argc; i++)
		printf("%s\n", argv[i]);

	return (0);
}
